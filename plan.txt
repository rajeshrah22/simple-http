Core functionality:

- Parse HTTP Request
- Path resolution
- Get resource
- Create response
- send response

HTTP Methods:

Only thing we will support

GET -

action:

get specified resource, and attach it to body of response using path. Query parameters?
We are going to ignore it as of right now.